<%= render partial: "shared/nav_bar", locals: {title: "Total expenses:", title_amount: @total} %>
<% @expenses.each do |expense| %>
  <%= expense.name %>
  <%= expense.amount %>
<% end %>

<%= link_to "Add new expense", new_group_expense_path(params[:group_id]) %>
