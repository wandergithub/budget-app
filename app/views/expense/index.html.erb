<%= render partial: "shared/nav_bar", locals: {title: "Total expenses:", title_amount: @total, back_button_path: group_index_path} %>
<% @expenses.each do |expense| %>
  <%= expense.name %>
  <%= expense.amount %>
<% end %>

<%= link_to "Add new expense", new_group_expense_path(params[:group_id]) %>
